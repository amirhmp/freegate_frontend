import RtlTextField from "@ui/components/common/RtlTextField";

const TestPage = () => {
  return (
    <div
      style={{
        display: "flex",
        padding: 8,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <RtlTextField />
    </div>
  );
};

export default TestPage;
